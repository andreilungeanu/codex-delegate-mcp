# Docs

- Never add content to README.md without explicit consent. Correcting something that
  became false is fine; new sentences, sections or feature bullets are not.
- The same applies to any user-facing doc when the change is additive rather than a fix.
